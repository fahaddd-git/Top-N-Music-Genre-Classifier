import pytest


@pytest.mark.xfail(reason="initial pipeline setup")
def test_sample():
    """Exists for initial ci pipeline setup.
    Add your tests somewhere in the tests dir and delete this"""
    assert False
